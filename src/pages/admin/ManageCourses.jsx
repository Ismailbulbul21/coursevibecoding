import AdminCourses from './Courses';

// This is a wrapper component to maintain backward compatibility
// for the App.jsx import while using the Courses component
const ManageCourses = AdminCourses;

export default ManageCourses; 